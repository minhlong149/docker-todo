{
  "name": "fs-docker-todo",
  "scripts": {
    "ubuntu": "docker container run -it ubuntu bash",
    "preserver": "docker build todo-backend -t todo-backend",
    "server": "docker run -p 3000:3000 todo-backend"
  }
}

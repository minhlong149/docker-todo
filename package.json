{
  "name": "fs-docker-todo",
  "scripts": {
    "ubuntu": "docker container run -it ubuntu bash",
    "server:docker": "cd todo-backend && npm run docker",
    "server:compose": "cd todo-backend && npm run compose"
  }
}
